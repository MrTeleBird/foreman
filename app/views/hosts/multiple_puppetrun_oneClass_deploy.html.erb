<%= render 'selected_hosts', :hosts => @hosts %>

<%= form_tag update_multiple_puppetrun_oneClass_deploy_hosts_path({:host_ids => params[:host_ids]}) do -%>
  <small><%= _('Deploy Single Class on the following hosts') %></small>
<% end %>
